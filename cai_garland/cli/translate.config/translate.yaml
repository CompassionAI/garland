defaults:
  - model: olive-cormorant-nllb-pooled-context
  # - model: nllb-with-context-for-zst
  - generation: model-segmenter-pooled-context
  - mode: interactive
  # - word_exclusion: bad-words-list
  - override hydra/job_logging: colorlog
  - override hydra/hydra_logging: colorlog
  - _self_

hydra:
  # verbose: cai_garland
  run:
    dir: .
  output_subdir: ~

cuda: False