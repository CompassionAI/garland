defaults:
  - model: olive-cormorant-bart
  - generation: medium-beam-long-inputs
  - mode: interactive
  - override hydra/job_logging: colorlog
  - override hydra/hydra_logging: colorlog
  - _self_

hydra:
  # verbose: cai_garland
  run:
    dir: .
  output_subdir: ~

cuda: False