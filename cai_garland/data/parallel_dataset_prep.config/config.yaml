defaults:
  - input: 84000-parallel-sentences
  - output: uncased-no-accents
  - stages:
    # - naive-unconcatted
    # - naive-concats
    - naive-concats-shuffled
    # - concatted-registers
    # - concatted-registers-shuffled
    # - unconcatted-registers-shuffled
    # - folio-registers
    # - dictionary
  - processing: local-dask
  - override hydra/job_logging: colorlog
  - override hydra/hydra_logging: colorlog
  - _self_

hydra:
  # verbose: cai_garland.data
  run:
    dir: .
  output_subdir: ~

seed: 12345

output_dir: ${oc.env:CAI_TEMP_PATH}/enbo_data

# These are used by Hugging Face models when shuffling
cuda: true
batch_size: 16

# Filter out Tibetan examples that tokenize longer than max_source_length
filter_longer_than_max_source_length: false

skip_duplicate_folio_check: false
