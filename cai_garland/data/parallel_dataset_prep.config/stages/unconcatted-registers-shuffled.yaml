defaults:
  - shuffle/shuffle@concatted-registers-shuffled
  - registers@concatted-registers-shuffled
  - concat/no-concat@concatted-registers-shuffled

concatted-registers-shuffled:
  # Function to pull the dataset
  pull_func:
    _target_: cai_garland.data.parallel_dataset_prep._pull_parallel_dataset

  # Function to prepare the dataset
  prep_func:
    _target_: cai_garland.data.parallel_dataset_prep._prep_concatted_register_dataset
